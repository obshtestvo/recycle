{

{% for i in spots %}
    "{{i.type.name}}": [
    {
	    "name": "{{i.name}}",
        "id"  : {{i.id}},
	    "description": "{{i.description}}",
	    "lat": {{i.lat}},
	    "lng": {{i.lng}}
    }
    ]
{% endfor %}

}
